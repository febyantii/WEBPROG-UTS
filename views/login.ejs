<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Login Admin</title>
  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: url('/background.png') no-repeat center center/cover;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .wrapper {
      display: flex;
      width: 1000px;
      height: 550px;
      border-radius: 20px;
      overflow: hidden;
    }

    /* === Bagian Form === */
    .form-section {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding-left: 60px; /* agak ke kiri */
    }

    .form-section h2 {
      font-size: 32px;
      text-align: center;
      margin-bottom: 8px;
      color: #ff3f6c;
    }

    .form-section p {
      text-align: center;
      margin-bottom: 25px;
      color: #333;
    }


    .input-box {
      width: 100%;
      padding: 14px;
      margin: 12px 0;
      border: none;
      border-radius: 15px;
      font-size: 14px;
      background: rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(15px);
      color: #000;
      box-shadow: 0 4px 20px rgba(0,0,0,0.15);
      box-sizing: border-box;
    }
    .password-group {
      position: relative;
      width: 100%;
    }
    .toggle-password {
      position: absolute;
      right: 15px;
      top: 50%;
      transform: translateY(-50%);
      cursor: pointer;
      width: 24px;
      height: 24px;
      object-fit: contain;
    }

    .btn-login {
      width: 50%;
      padding: 14px;
      margin: 18px auto 0px; /* jarak tetap sama */
      display: block;
      border: none;
      border-radius: 15px;
      background: #ff6f91;
      color: #fff;
      font-weight: bold;
      font-size: 15px;
      cursor: pointer;
      box-shadow: 0 6px 15px rgba(255,111,145,0.4);
      transition: 0.3s;
      box-sizing: border-box;
    }
    .btn-login:hover {
      background: #ff3f6c;
    }
    
    .register-link {
      margin-top: 18px;
      font-size: 14px;
      text-align: center;
      color: #333;
    }
    .register-link a {
      color: #ff3f6c;
      font-weight: bold;
      text-decoration: none;
    }

    /* === Bagian Info / Logo === */
    .info-section {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 40px;
    }

    .info-section img {
      width: 130px;
      margin-bottom: 20px;
    }

    .info-section h1 {
      font-size: 28px;
      margin-bottom: 15px;
      color: #000; /* hitam biar jelas */
    }

    .info-section p {
      font-size: 15px;
      line-height: 1.6;
      max-width: 320px;
      color: #000;
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <div class="form-section">
      <h2>Hello!</h2>
      <p>Sign in to your account</p>

      <% if (error) { %>
        <p style="color:red; text-align:center;"><%= error %></p>
      <% } %>

      <form method="POST" action="/login">
        <input type="text" name="username" placeholder="Username" class="input-box" required>
        <div class="password-group">
          <input type="password" id="password" name="password" placeholder="Password" class="input-box" required>
          <img src="/eye-closed.png" alt="Toggle" class="toggle-password" onclick="togglePassword('password', this)"> 
        </div> 
        <button type="submit" class="btn-login">SIGN IN</button>
      </form>

      <div class="register-link">
        Don’t have an account? <a href="/register">Create</a>
      </div>
    </div>

    <div class="info-section">
      <img src="/logo.png" alt="Logo">
      <h1>Welcome to Chun Qing Learning Center!</h1>
      <p>Selamat datang di panel admin Chun Qing.  
        Kelola akun dan data dengan mudah melalui sistem ini.</p>
    </div>
    <script>
      function togglePassword(id, icon) {
        const input = document.getElementById(id);
        if (input.type === "password") {
          input.type = "text";
          icon.src = "/eye-open.png"; // ganti ke ikon mata terbuka
        } else {
          input.type = "password";
          icon.src = "/eye-closed.png"; // ganti ke ikon mata tertutup
        }
      }
    </script>  
  </div>
</body>
</html>
