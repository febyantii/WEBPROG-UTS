<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Register Admin</title>
  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: url('/background.png') no-repeat center center/cover;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .wrapper {
      display: flex;
      width: 1000px;
      height: 550px;
      border-radius: 20px;
      overflow: hidden;
    }

    .form-section {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding-left: 60px;
    }

    .form-section h2 {
      font-size: 32px;
      text-align: center;
      margin-bottom: 8px;
      color: #ff3f6c;
    }

    .form-section p {
      text-align: center;
      margin-bottom: 16px;
      color: #333;
    }

    .input-box {
      width: 100%;
      padding: 14px 45px 14px 14px;
      margin: 5px 0;
      border: none;
      border-radius: 15px;
      font-size: 14px;
      background: rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(15px);
      color: #000;
      box-shadow: 0 4px 20px rgba(0,0,0,0.15);
      box-sizing: border-box;
    }

    .name-group {
      display: flex;
      gap: 10px;
      width: 100%;
    }

    .name-group input {
      flex: 1;
      margin: 12px 0;
    }

    .password-group {
      position: relative;
      width: 100%;
    }

    .toggle-password {
      position: absolute;
      right: 15px;
      top: 50%;
      transform: translateY(-50%);
      cursor: pointer;
      width: 24px;
      height: 24px;
      object-fit: contain;
    }

    .btn-register {
      width: 50%;
      padding: 14px;
      margin: 18px auto 0px;
      display: block;
      border: none;
      border-radius: 15px;
      background: #ff6f91;
      color: #fff;
      font-weight: bold;
      font-size: 15px;
      cursor: pointer;
      box-shadow: 0 6px 15px rgba(255,111,145,0.4);
      transition: 0.3s;
      box-sizing: border-box;
    }
    .btn-register:hover {
      background: #ff3f6c;
    }

    .back-link {
      margin-top: 18px;
      font-size: 14px;
      text-align: center;
      color: #333;
    }
    .back-link a {
      color: #ff3f6c;
      font-weight: bold;
      text-decoration: none;
    }

    .info-section {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 40px;
      background: #fff;
    }

    .info-section img {
      width: 130px;
      margin-bottom: 20px;
    }

    .info-section h1 {
      font-size: 28px;
      margin-bottom: 15px;
      color: #000;
    }

    .info-section p {
      font-size: 15px;
      line-height: 1.6;
      max-width: 320px;
      color: #000;
    }

    .error {
      color: red;
      font-size: 13px;
      margin-bottom: 10px;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <div class="form-section">
      <h2>Create Account</h2>
      <p>Register your admin account</p>

      <form method="POST" action="/register">
        <div class="name-group">
          <input type="text" name="firstname" placeholder="Nama Depan" class="input-box" required>
          <input type="text" name="lastname" placeholder="Nama Belakang" class="input-box" required>
        </div>

        <input type="text" name="username" placeholder="Username" class="input-box" required>

        <div class="password-group">
          <input type="password" id="password" name="password" placeholder="Password" class="input-box" required>
          <img src="/eye-closed.png" alt="Toggle" class="toggle-password" onclick="togglePassword('password', this)"> 
        </div> 
        
        <div class="password-group">
          <input type="password" id="password2" name="password2" placeholder="Konfirmasi Password" class="input-box" required>
          <img src="/eye-closed.png" alt="Toggle" class="toggle-password" onclick="togglePassword('password2', this)">
        </div>
        

        <% if (error) { %>
          <p style="color:red; text-align:center;" class="error"><%= error %></p>
        <% } %>
  

        <button type="submit" class="btn-register">REGISTER</button>
      </form>

      <div class="back-link">
        <a href="/">⬅ Back to Login</a>
      </div>
    </div>

    <div class="info-section">
      <img src="/logo.png" alt="Logo">
      <h1>Welcome to Chun Qing Learning Center</h1>
      <p>Daftarkan akun admin baru untuk mulai mengelola sistem Chun Qing Learning Center.  
        Mudah, cepat, dan aman!</p>
    </div>
  </div>
  <script>
    function togglePassword(id, icon) {
      const input = document.getElementById(id);
      if (input.type === "password") {
        input.type = "text";
        icon.src = "/eye-open.png"; // ganti ke ikon mata terbuka
      } else {
        input.type = "password";
        icon.src = "/eye-closed.png"; // ganti ke ikon mata tertutup
      }
    }
  </script>
  
</body>
</html>
