<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Register Admin</title>
  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: url('/background.png') no-repeat center center/cover;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .wrapper {
      display: flex;
      width: 1000px;
      height: 550px;
      border-radius: 20px;
      overflow: hidden;
    }

    /* === Bagian Form === */
    .form-section {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding-left: 60px;
    }

    .form-section h2 {
      font-size: 32px;
      text-align: center;
      margin-bottom: 8px;
      color: #ff3f6c;
    }

    .form-section p {
      text-align: center;
      margin-bottom: 25px;
      color: #333;
    }

    .input-box {
      width: 90%;
      padding: 14px;
      margin: 12px 0;
      border: none;
      border-radius: 15px;
      font-size: 14px;
      background: rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(15px);
      color: #000;
      box-shadow: 0 4px 20px rgba(0,0,0,0.15);
      box-sizing: border-box;
    }

    .btn-register {
      width: 90%;
      padding: 14px;
      margin-top: 18px; /* jarak asli biar tetap ada space lebih */
      border: none;
      border-radius: 15px;
      background: #ff6f91;
      color: #fff;
      font-weight: bold;
      font-size: 15px;
      cursor: pointer;
      box-shadow: 0 6px 15px rgba(255,111,145,0.4);
      transition: 0.3s;
      box-sizing: border-box;
    }
    .btn-register:hover {
      background: #ff3f6c;
    }

    .back-link {
      margin-top: 18px;
      font-size: 14px;
      text-align: center;
      color: #333;
    }
    .back-link a {
      color: #ff3f6c;
      font-weight: bold;
      text-decoration: none;
    }

    /* === Bagian Info / Logo === */
    .info-section {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 40px;
    }

    .info-section img {
      width: 130px;
      margin-bottom: 20px;
    }

    .info-section h1 {
      font-size: 28px;
      margin-bottom: 15px;
      color: #000;
    }

    .info-section p {
      font-size: 15px;
      line-height: 1.6;
      max-width: 320px;
      color: #000;
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <div class="form-section">
      <h2>Create Account</h2>
      <p>Register your admin account</p>

      <% if (error) { %>
        <p style="color:red;"><%= error %></p>
      <% } %>

      <form method="POST" action="/register">
        <input type="text" name="username" placeholder="Username" class="input-box" required>
        <input type="password" name="password" placeholder="Password" class="input-box" required>
        <input type="password" name="password2" placeholder="Konfirmasi Password" class="input-box" required>
        <button type="submit" class="btn-register">REGISTER</button>
      </form>

      <div class="back-link">
        <a href="/">⬅ Back to Login</a>
      </div>
    </div>

    <div class="info-section">
      <img src="/logo.png" alt="Logo">
      <h1>Welcome to CHUNQING</h1>
      <p>Daftarkan akun admin baru untuk mulai mengelola sistem CHUNQING.  
        Mudah, cepat, dan aman!</p>
    </div>
  </div>
</body>
</html>
